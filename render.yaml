services:
  - type: web
    name: telegram-tts-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn bot:app --host 0.0.0.0 --port 5000
    plan: free
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
